apiVersion: v1
kind: Pod
metadata:
  name: volume-anomaly-tester
  namespace: anomaly-test
  labels:
    app: volume-anomaly-tester
spec:
  restartPolicy: Never
  containers:
    - name: volume-anomaly-tester
      image: ythwork/volume-anomaly-tester:0.0.1
      imagePullPolicy: IfNotPresent